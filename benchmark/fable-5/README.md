# Fable 5 outputs

Drop model outputs here for side-by-side comparison on the [benchmark page](../../benchmark.html).

## Files per prompting style

| Style | Path | Philosophy |
|-------|------|------------|
| Carving with Stone | `style-1/` | `output.html`, `output.svg` |
| Carving with Clay | `style-2/` | `output.html`, `output.svg` |

Replace the placeholder files with your actual Fable 5 results.
